{
  "name": "tachi-stereo-lab",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4188 --strictPort",
    "build": "vite build",
    "test": "node --test tests/*.test.mjs",
    "verify": "node verify-browser.mjs",
    "models": "node prepare-model.mjs",
    "samples": "node prepare-samples.mjs"
  },
  "dependencies": {
    "@huggingface/transformers": "3.8.1"
  },
  "devDependencies": {
    "playwright": "1.58.2",
    "vite": "7.3.6"
  },
  "overrides": {
    "sharp": "0.35.4"
  }
}
